<view class="tabs">
  <view
    class="tab {{current === index ? 'active' : ''}}"
    bind:tap="handleSwitch"
    wx:for="{{data}}"
    wx:key="{{item.id}}"
    data-index="{{index}}"
  >
    {{item.name}}
  </view>

  <view class="tab-scrollbar tab-{{data.length}}" />
</view>

